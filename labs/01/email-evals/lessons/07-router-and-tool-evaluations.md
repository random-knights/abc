# Lesson 07: Router And Draft Evaluations

Run `course.py 07 --provider both`. The suite checks the complete tool sequence,
campaign id fidelity, email format, deterministic safety, drafts-only behavior,
quality, and grounding.

Hard rules run before model judgment. An LLM judge cannot waive an invented
relationship, email address, link, or unsupported number.
